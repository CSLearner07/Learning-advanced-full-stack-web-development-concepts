//1. Write the program to greet a person given their first and last name

function greetPerson(firstName, lastName) {
  return `Hello , ${firstName} ${lastName}`;
}

const greet = greetPerson("john", "doe");

console.log(greet);
